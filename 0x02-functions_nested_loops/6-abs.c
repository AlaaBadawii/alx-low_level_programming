#include "main.h"
#include <stdio.h>
/**
 * _abs - computes the absolute value of an integer.
 * @int: int input
 * Return: absolute value.
 */
int _abs(int)
{
	if (n >= 0)
	{
		_putchar(n);
	}
	elae
	{
		n = n * -1;
		_putchar (n);
	}
}
